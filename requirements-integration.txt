# D 통합티어 (LAKATOS_IT) 전용 추가 의존 — hermetic 단위 suite(requirements.txt)와 분리.
# 실 Neo4j+PG 를 testcontainers 로 띄워 kg_tx(ROB-1) 롤백/복구 모델을 실 DB 로 characterize.
# 사용: uv pip install -r requirements.txt -r requirements-integration.txt
#       LAKATOS_IT=1 pytest -m integration
# (이 파일은 추가 tier만 담다. neo4j/psycopg2/pytest는 requirements.txt 필수.)
testcontainers[neo4j,postgres]==4.14.2
